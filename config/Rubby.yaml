
proxy-providers:
  AKUN1:
    type: file
    path: "./proxy_provider/Akun/AKUN1.yaml"
    health-check:
      enable: true
      url: 'http://www.gstatic.com/generate_204'
      interval: 300
  AKUN2:
    type: file
    path: "./proxy_provider/Akun/AKUN2.yaml"
    health-check:
      enable: true
      url: 'http://www.gstatic.com/generate_204'
      interval: 300
  AKUN3:
    type: file
    path: "./proxy_provider/Akun/AKUN3.yaml"
    health-check:
      enable: true
      url: 'http://www.gstatic.com/generate_204'
      interval: 300
  AKUN4:
    type: file
    path: "./proxy_provider/Akun/AKUN4.yaml"
    health-check:
      enable: true
      url: 'http://www.gstatic.com/generate_204'
      interval: 300

proxy-groups:

- name: AKUN1
  type: select
  disable-udp: false
  use:
  - AKUN1
  url: 'http://cp.cloudflare.com/generate_204'
  interval: '300'

- name: AKUN2
  type: select
  disable-udp: false
  use:
  - AKUN2
  url: 'http://cp.cloudflare.com/generate_204'
  interval: '300'
- name: AKUN3
  type: select
  disable-udp: false
  use:
  - AKUN3
  url: 'http://cp.cloudflare.com/generate_204'
  interval: '300'
- name: AKUN4
  type: select
  disable-udp: false
  use:
  - AKUN4
  url: 'http://cp.cloudflare.com/generate_204'
  interval: '300'
  
- name: Relay Proxy
  type: relay
  disable-udp: false
  proxies:
  - AKUN1
  - AKUN2
  url: 'http://cp.cloudflare.com/generate_204'
  interval: '300'
  
- name: ETH1
  type: select
  interface-name: eth1
  disable-udp: false
  proxies:
  - AKUN1
  - AKUN2
  - AKUN3
  - AKUN4
  - Relay Proxy
  - Fallback-Akun1
  - Fallback-Akun2
  - Fallback-Akun3
  - Fallback-Akun4
  - Ping-Akun1
  - Ping-Akun2
  - Ping-Akun3
  - Ping-Akun4 
  - REJECT
  - DIRECT
  url: 'http://www.gstatic.com/generate_204'
  interval: '300'

- name: USB0
  type: select
  interface-name: usb0
  disable-udp: false
  proxies:
  - AKUN1
  - AKUN2
  - AKUN3
  - AKUN4
  - Relay Proxy
  - Fallback-Akun1
  - Fallback-Akun2
  - Fallback-Akun3
  - Fallback-Akun4
  - Ping-Akun1
  - Ping-Akun2
  - Ping-Akun3
  - Ping-Akun4 
  - REJECT
  - DIRECT
  url: 'http://www.gstatic.com/generate_204'
  interval: '300'
  
- name: Loadbalance ETH1 & USB0
  type: load-balance
  strategy: round-robin
  disable-udp: false
  proxies:
  - ETH1
  - USB0
  url: 'http://www.gstatic.com/generate_204'
  interval: '300'
  
- name: Home ðŸŒ
  type: select
  disable-udp: false
  proxies:
  - AKUN1
  - AKUN2
  - AKUN3
  - AKUN4
  - Relay Proxy
  - Fallback-Akun1
  - Fallback-Akun2
  - Fallback-Akun3
  - Fallback-Akun4
  - Ping-Akun1
  - Ping-Akun2
  - Ping-Akun3
  - Ping-Akun4 
  - Loadbalance AKUN1
  - Loadbalance AKUN2
  - Loadbalance AKUN3
  - Loadbalance AKUN4
  - Loadbalance ETH1 & USB0
  - Streaming ðŸŽ¬
  - Sosmed ðŸ””
  - Online Shop ðŸ›’
  - Games ðŸŽ®
  - MBanking ðŸ’²
  - WHATSAPP ONLY ðŸ“ž
  - ETH1
  - USB0
  - REJECT
  - DIRECT
  url: 'http://cp.cloudflare.com/generate_204'
  interval: '300'

- name: Streaming ðŸŽ¬
  type: select
  disable-udp: false
  proxies:
  - AKUN1
  - AKUN2
  - AKUN3
  - AKUN4
  - Fallback-Akun1
  - Fallback-Akun2
  - Fallback-Akun3
  - Fallback-Akun4
  - Ping-Akun1
  - Ping-Akun2
  - Ping-Akun3
  - Ping-Akun4 
  - Loadbalance AKUN1
  - Loadbalance AKUN2
  - Loadbalance AKUN3
  - Loadbalance AKUN4
  - Loadbalance ETH1 & USB0
  url: 'http://cp.cloudflare.com/generate_204'
  interval: '300'

- name: Sosmed ðŸ””
  type: select
  disable-udp: false
  proxies:
  - AKUN1
  - AKUN2
  - AKUN3
  - AKUN4
  - Fallback-Akun1
  - Fallback-Akun2
  - Fallback-Akun3
  - Fallback-Akun4
  - Ping-Akun1
  - Ping-Akun2
  - Ping-Akun3
  - Ping-Akun4 
  - Loadbalance AKUN1
  - Loadbalance AKUN2
  - Loadbalance AKUN3
  - Loadbalance AKUN4
  - Loadbalance ETH1 & USB0
  url: 'http://cp.cloudflare.com/generate_204'
  interval: '300'

- name: Online Shop ðŸ›’
  type: select
  disable-udp: false
  proxies:
  - AKUN1
  - AKUN2
  - AKUN3
  - AKUN4
  - Fallback-Akun1
  - Fallback-Akun2
  - Fallback-Akun3
  - Fallback-Akun4
  - Ping-Akun1
  - Ping-Akun2
  - Ping-Akun3
  - Ping-Akun4 
  - Loadbalance AKUN1
  - Loadbalance AKUN2
  - Loadbalance AKUN3
  - Loadbalance AKUN4
  - Loadbalance ETH1 & USB0
  url: 'http://cp.cloudflare.com/generate_204'
  interval: '300'

- name: MBanking ðŸ’²
  type: select
  disable-udp: false
  proxies:
  - AKUN1
  - AKUN2
  - AKUN3
  - AKUN4
  - Fallback-Akun1
  - Fallback-Akun2
  - Fallback-Akun3
  - Fallback-Akun4
  - Ping-Akun1
  - Ping-Akun2
  - Ping-Akun3
  - Ping-Akun4 
  - Loadbalance AKUN1
  - Loadbalance AKUN2
  - Loadbalance AKUN3
  - Loadbalance AKUN4
  - Loadbalance ETH1 & USB0
  url: 'http://cp.cloudflare.com/generate_204'
  interval: '300'

- name: Games ðŸŽ®
  type: select
  disable-udp: false
  proxies:
  - AKUN1
  - AKUN2
  - AKUN3
  - AKUN4
  - Relay Proxy
  - Fallback-Akun1
  - Fallback-Akun2
  - Fallback-Akun3
  - Fallback-Akun4
  - Ping-Akun1
  - Ping-Akun2
  - Ping-Akun3
  - Ping-Akun4 
  - Loadbalance AKUN1
  - Loadbalance AKUN2
  - Loadbalance AKUN3
  - Loadbalance AKUN4
  - Loadbalance ETH1 & USB0
  url: 'http://cp.cloudflare.com/generate_204'
  interval: '300'

- name: WHATSAPP ONLY ðŸ“ž
  type: select
  disable-udp: false
  proxies:
  - AKUN1
  - AKUN2
  - AKUN3
  - AKUN4
  - Relay Proxy
  - Fallback-Akun1
  - Fallback-Akun2
  - Fallback-Akun3
  - Fallback-Akun4
  - Ping-Akun1
  - Ping-Akun2
  - Ping-Akun3
  - Ping-Akun4 
  - Loadbalance ETH1 & USB0
  - DIRECT
  url: 'http://cp.cloudflare.com/generate_204'
  interval: '300'
#------------++-----++--ETC--++----++-------------#
- name: Fallback-Akun1
  type: fallback
  disable-udp: false
  use:
  - AKUN1
  url: 'http://www.gstatic.com/generate_204'
  interval: '99'
- name: Fallback-Akun2
  type: fallback
  disable-udp: false
  use:
  - AKUN2
  url: 'http://www.gstatic.com/generate_204'
  interval: '99'
- name: Fallback-Akun3
  type: fallback
  disable-udp: false
  use:
  - AKUN3
  url: 'http://www.gstatic.com/generate_204'
  interval: '99'
- name: Fallback-Akun4
  type: fallback
  disable-udp: false
  use:
  - AKUN4
  url: 'http://www.gstatic.com/generate_204'
  interval: '99'
#-------------------------------------------------#
- name: Ping-Akun1
  type: url-test
  disable-udp: false
  use:
  - AKUN1
  url: 'http://www.gstatic.com/generate_204'
  interval: '99'
  tolerance: '99'
- name: Ping-Akun2
  type: url-test
  disable-udp: false
  use:
  - AKUN2
  url: 'http://www.gstatic.com/generate_204'
  interval: '99'
  tolerance: '99'
- name: Ping-Akun3
  type: url-test
  disable-udp: false
  use:
  - AKUN3
  url: 'http://www.gstatic.com/generate_204'
  interval: '99'
  tolerance: '99'
- name: Ping-Akun4
  type: url-test
  disable-udp: false
  use:
  - AKUN4
  url: 'http://www.gstatic.com/generate_204'
  interval: '99'
  tolerance: '99'
#-------------------------------------------------#
- name: Loadbalance AKUN1
  type: load-balance
  strategy: round-robin
  disable-udp: false
  use:
  - AKUN1
  url: 'http://www.gstatic.com/generate_204'
  interval: '99'
- name: Loadbalance AKUN2
  type: load-balance
  strategy: round-robin
  disable-udp: false
  use:
  - AKUN2
  url: 'http://www.gstatic.com/generate_204'
  interval: '99'
- name: Loadbalance AKUN3
  type: load-balance
  strategy: round-robin
  disable-udp: false
  use:
  - AKUN3
  url: 'http://www.gstatic.com/generate_204'
  interval: '99'
- name: Loadbalance AKUN4
  type: load-balance
  strategy: round-robin
  disable-udp: false
  use:
  - AKUN4
  url: 'http://www.gstatic.com/generate_204'
  interval: '99'

#-------------------------------------------------#
- name: BLOCK ADS
  type: select
  disable-udp: false
  proxies:
  - REJECT
  - Home ðŸŒ
  - Loadbalance ETH1 & USB0
  url: 'http://www.gstatic.com/generate_204'
  interval: '300'
#-------------------------------------------------#
rule-providers:
  Gamemaster:
    type: file
    behavior: classical
    path: ./rule_provider/rule-port-game.yaml
  Sosmedmaster:
    type: file
    behavior: classical
    path: ./rule_provider/rule-port-sosmed.yaml
  Streammaster:
    type: file
    behavior: classical
    path: ./rule_provider/rule-port-stream.yaml
  Olshopmaster:
    type: file
    behavior: classical
    path: ./rule_provider/rule-port-olshop.yaml
  Bankmaster:
    type: file
    behavior: classical
    path: ./rule_provider/rule-port-bank.yaml
  Whatsappmaster:
    type: file
    behavior: classical
    path: ./rule_provider/rule-port-whatsapp.yaml
  rule_BlockAds:
    type: file
    behavior: classical
    path: "./rule_provider/rule-myads.yaml"

script:
  code: |
    def main(ctx, metadata):
        directkeywordlist = ["youtube"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-STREAM' % directkeyword)
                return "Streaming ðŸŽ¬"

        directkeywordlist = ["ytimg"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-STREAM' % directkeyword)
                return "Streaming ðŸŽ¬"

        directkeywordlist = ["googlevideo"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-STREAM' % directkeyword)
                return "Streaming ðŸŽ¬"

        directkeywordlist = ["googleapis"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-STREAM' % directkeyword)
                return "Streaming ðŸŽ¬"

        directkeywordlist = ["ggpht"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-STREAM' % directkeyword)
                return "Streaming ðŸŽ¬"

        directkeywordlist = ["gvt1"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-STREAM' % directkeyword)
                return "Streaming ðŸŽ¬"

        directkeywordlist = ["gvt2"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-STREAM' % directkeyword)
                return "Streaming ðŸŽ¬"

        directkeywordlist = ["obs"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-STREAM' % directkeyword)
                return "Streaming ðŸŽ¬"

        directkeywordlist = ["steam"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-STREAM' % directkeyword)
                return "Streaming ðŸŽ¬"

        directkeywordlist = ["netflix"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-STREAM' % directkeyword)
                return "Streaming ðŸŽ¬"

        directkeywordlist = ["hotstar"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-STREAM' % directkeyword)
                return "Streaming ðŸŽ¬"

        directkeywordlist = ["disney"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-STREAM' % directkeyword)
                return "Streaming ðŸŽ¬"

        directkeywordlist = ["facebook"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-SOSMED' % directkeyword)
                return "Sosmed ðŸ””"

        directkeywordlist = ["fbcdn"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-SOSMED' % directkeyword)
                return "Sosmed ðŸ””"

        directkeywordlist = ["fbsx"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-SOSMED' % directkeyword)
                return "Sosmed ðŸ””"

        directkeywordlist = ["instagram"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-SOSMED' % directkeyword)
                return "Sosmed ðŸ””"

        directkeywordlist = ["cdninstagram"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-SOSMED' % directkeyword)
                return "Sosmed ðŸ””"

        directkeywordlist = ["twitter"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-SOSMED' % directkeyword)
                return "Sosmed ðŸ””"

        directkeywordlist = ["tiktok"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-SOSMED' % directkeyword)
                return "Sosmed ðŸ””"

        directkeywordlist = ["tiktokv"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-SOSMED' % directkeyword)
                return "Sosmed ðŸ””"

        directkeywordlist = ["tiktokcdn"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-SOSMED' % directkeyword)
                return "Sosmed ðŸ””"

        directkeywordlist = ["byteoversea"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-SOSMED' % directkeyword)
                return "Sosmed ðŸ””"

        directkeywordlist = ["ibytedtos"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-SOSMED' % directkeyword)
                return "Sosmed ðŸ””"

        directkeywordlist = ["ipstatp"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-SOSMED' % directkeyword)
                return "Sosmed ðŸ””"

        directkeywordlist = ["muscdn"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-SOSMED' % directkeyword)
                return "Sosmed ðŸ””"

        directkeywordlist = ["musical"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-SOSMED' % directkeyword)
                return "Sosmed ðŸ””"

        directkeywordlist = ["shopee"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-OLSHOP' % directkeyword)
                return "Online Shop ðŸ›’"

        directkeywordlist = ["shopeemobile"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-OLSHOP' % directkeyword)
                return "Online Shop ðŸ›’"

        directkeywordlist = ["bukalapak"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-OLSHOP' % directkeyword)
                return "Online Shop ðŸ›’"

        directkeywordlist = ["lazada"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-OLSHOP' % directkeyword)
                return "Online Shop ðŸ›’"

        directkeywordlist = ["tokopedia"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-OLSHOP' % directkeyword)
                return "Online Shop ðŸ›’"

        directkeywordlist = ["alicdn"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-OLSHOP' % directkeyword)
                return "Online Shop ðŸ›’"

        directkeywordlist = ["alibaba"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-OLSHOP' % directkeyword)
                return "Online Shop ðŸ›’"

        directkeywordlist = ["amazon"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-OLSHOP' % directkeyword)
                return "Online Shop ðŸ›’"

        directkeywordlist = ["aliyun-inc"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-OLSHOP' % directkeyword)
                return "Online Shop ðŸ›’"

        directkeywordlist = ["aliyun"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-OLSHOP' % directkeyword)
                return "Online Shop ðŸ›’"

        directkeywordlist = ["taobao"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-OLSHOP' % directkeyword)
                return "Online Shop ðŸ›’"

        directkeywordlist = ["telegram"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-SIMASTER-V1.8' % directkeyword)
                return "Home ðŸŒ"

        directkeywordlist = ["roblox"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["aov"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["lytogame"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-SIMASTER-V1.8' % directkeyword)
                return "Home ðŸŒ"

        directkeywordlist = ["whatsapp"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-WHATSAPP' % directkeyword)
                return "WHATSAPP ONLY ðŸ“ž"

        directkeywordlist = ["app"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-BANK' % directkeyword)
                return "MBanking ðŸ’²"

        directkeywordlist = ["dana"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-BANK' % directkeyword)
                return "MBanking ðŸ’²"

        directkeywordlist = ["gopay"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-BANK' % directkeyword)
                return "MBanking ðŸ’²"

        directkeywordlist = ["onetrust"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-BANK' % directkeyword)
                return "MBanking ðŸ’²"

        directkeywordlist = ["lencr"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-BANK' % directkeyword)
                return "MBanking ðŸ’²"

        directkeywordlist = ["ingest"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-BANK' % directkeyword)
                return "MBanking ðŸ’²"

        directkeywordlist = ["bri"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-BANK' % directkeyword)
                return "MBanking ðŸ’²"

        directkeywordlist = ["bca"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-BANK' % directkeyword)
                return "MBanking ðŸ’²"

        directkeywordlist = ["bni"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-BANK' % directkeyword)
                return "MBanking ðŸ’²"

        directkeywordlist = ["mandiri"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-BANK' % directkeyword)
                return "MBanking ðŸ’²"

        directkeywordlist = ["klikbca"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-BANK' % directkeyword)
                return "MBanking ðŸ’²"

        directkeywordlist = ["bca"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-BANK' % directkeyword)
                return "MBanking ðŸ’²"

        directkeywordlist = ["livechat"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-BANK' % directkeyword)
                return "MBanking ðŸ’²"

        directkeywordlist = ["sectigo"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-BANK' % directkeyword)
                return "MBanking ðŸ’²"

        directkeywordlist = ["bankmandiri"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-BANK' % directkeyword)
                return "MBanking ðŸ’²"

        directkeywordlist = ["datadoghq-browser-agent"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-BANK' % directkeyword)
                return "MBanking ðŸ’²"

        directkeywordlist = ["ovo"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-BANK' % directkeyword)
                return "MBanking ðŸ’²"

        directkeywordlist = ["paypal"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-BANK' % directkeyword)
                return "MBanking ðŸ’²"

        directkeywordlist = ["indodax"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-BANK' % directkeyword)
                return "MBanking ðŸ’²"

        directkeywordlist = ["aihelp"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["yuanzhapp"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["mobilelegends"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["garena"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["garenanow"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["akamaized"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["akamaihd"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["stumble"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["exitgames"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["unistream"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["kitkagames"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["kitkagames"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["unity3d"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["microvirt"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["supersonicads"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["gameanalytics"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["applovin"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["pubg"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["tencent"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["krafton"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["anticheatexpert"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["neptunegame"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["bosbosstatic"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["bytegsdk"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["youngjoygame"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["ibytedtos"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["byteoversea"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["freefiremobile"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["kitkagames"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["rbxcdn"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["roblox"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["mcafee"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-KHUSUS-GAME' % directkeyword)
                return "Games ðŸŽ®"

        directkeywordlist = ["zerotier"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use ZEROTIER CONNECTION' % directkeyword)
                return "ZEROTIER CONNECTION"

        directkeywordlist = ["mikrotik"]
        for directkeyword in directkeywordlist:
            if directkeyword in metadata["host"]:
                ctx.log('[Script] matched keyword %s use PORT-MIKROTIK' % directkeyword)
                return "PORT-MIKROTIK"

        port = int(metadata["dst_port"])
        if metadata["network"] == "UDP":
            if port == "21,22,23,53,80,81,443,853,5353,8000,8008,8080,8081,8090,8443,8888":
                ctx.log('[Script] matched QUIC traffic use Home ðŸŒ')
                return "Home ðŸŒ"

        port = int(metadata["dst_port"])
        if metadata["network"] == "UDP":
            if port == "9993":
                ctx.log('[Script] matched QUIC traffic use ZEROTIER CONNECTION"')
                return "ZEROTIER CONNECTION"

        port = int(metadata["dst_port"])
        if metadata["network"] == "UDP":
            if port == "30000":
                ctx.log('[Script] matched QUIC traffic use ZEROTIER CONNECTION"')
                return "ZEROTIER CONNECTION"

        if metadata["dst_ip"] == "192.168.1.1":
            metadata["dst_ip"] = ctx.resolve_ip(metadata["host"])

        port_list = [21, 22, 23, 53, 80, 81, 443, 853, 5353, 8000, 8008, 8080, 8081, 8090, 8443, 8888]
        if port not in port_list:
            ctx.log('[Script] not common port use Games ðŸŽ®')
            return "Games ðŸŽ®"

        port_list = [3478, 5222]
        if port in port_list:
            ctx.log('[Script] common port use WHATSAPP ONLY ðŸ“ž')
            return "WHATSAPP ONLY ðŸ“ž"

        port_list = [30000]
        if port in port_list:
            ctx.log('[Script] common port use PORT-MIKROTIK')
            return "PORT MIKROTIK"

        port_list = [20443]
        if port in port_list:
            ctx.log('[Script] common port use Online Shop ðŸ›’')
            return "Online Shop ðŸ›’"

        if metadata["dst_ip"] == "":
            return "Home ðŸŒ"

        code = ctx.geoip(metadata["dst_ip"])
        if code == "ID":
            ctx.log('[Script] Geoip ID')
            return "Home ðŸŒ"

        ctx.log('[Script] Home ðŸŒ')
        return "Home ðŸŒ"
        
rules:
- RULE-SET,rule_BlockAds,BLOCK ADS
- IP-CIDR,198.18.0.1/16,REJECT,no-resolve
- RULE-SET,Gamemaster,Games ðŸŽ®
- RULE-SET,Sosmedmaster,Sosmed ðŸ””
- RULE-SET,Streammaster,Streaming ðŸŽ¬
- RULE-SET,Olshopmaster,Online Shop ðŸ›’
- RULE-SET,Bankmaster,MBanking ðŸ’²
- RULE-SET,Whatsappmaster,WHATSAPP ONLY ðŸ“ž
- MATCH,Home ðŸŒ
